using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace LitJson.Example
{
    public class Example : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            //序列化
            //  var jsonContent = JsonMapper.ToJson(T);
            //反序列化
            //  var striptableObj = JsonMapper.ToObject<T>(string);
        }
    }
}
